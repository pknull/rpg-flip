# Decisions

- Casts satisfy the `Castable` Protocol; `Tosser` validates SIDES and toss
  counts up front rather than failing mid-roll.
- Memory/ is exactly the v2 pair; `.asha/config.json` is tracked; harness
  state and event logs stay local.
